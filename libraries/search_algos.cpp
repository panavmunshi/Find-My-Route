#include "search_algos.h"

using namespace cust_lib;

vector<Edge> search_algos::a_star_search(Graph* search_graph, Vertex source, Vertex destination) {
    return vector<Edge>();
}

vector<Edge> search_algos::reconstruct_path(Vertex source, Vertex destination) {
    return vector<Edge>();
}